# Super Strong - Фитнес Трекер

Мобильное веб-приложение для отслеживания тренировок, разработанное как Telegram Web App с поддержкой обычной аутентификации по username. Приложение позволяет вести дневник тренировок, отслеживать прогресс и анализировать статистику.

## 🎯 Функциональность

### Основные возможности
- **Ведение дневника тренировок** - записывайте упражнения, подходы, повторения и вес для каждого дня
- **Календарь тренировок** - визуализация дней с тренировками в виде календаря
- **Статистика профиля** - отслеживание общего поднятого веса, количества тренировочных дней, времени использования приложения
- **Каталог упражнений** - база упражнений с категоризацией (грудь, спина, ноги и т.д.)
- **Синхронизация с сервером** - все данные сохраняются на сервере в реальном времени
- **Поддержка Telegram** - интеграция с Telegram Web App для удобного использования в мессенджере
- **Аутентификация** - вход через Telegram или по username

### Интерфейс
- **Страница календаря** - просмотр истории тренировок по датам
- **Страница упражнений** - выбор упражнений для текущей тренировки
- **Страница отслеживания** - занесение результатов (подходы, повторения, вес)
- **Профиль** - просмотр личной статистики
- **Настройки** - управление аккаунтом (выход, удаление профиля)

## 🛠 Требования

- **Node.js** 18+ или **nvm** (Node Version Manager)
- **pnpm** (рекомендуется вместо npm)
- **Docker** и **Docker Compose** (для локального Directus)

## 📦 Установка

### 1. Клонировать репозиторий
```bash
git clone <repository-url>
cd super-strong
```

### 2. Установить зависимости
```bash
pnpm install
```

### 3. Создать файл окружения
Создайте файл `.env.local` в корне проекта:
```env
VITE_API_URL=http://localhost:8055
```

Это адрес локального Directus в Docker.

## 🚀 Запуск локально

### Терминал 1 - Запустить фронтенд (приложение)
```bash
pnpm dev
```
Приложение откроется на `http://localhost:5173`

### Терминал 2 - Запустить Docker контейнеры (Directus + PostgreSQL)
```bash
docker-compose up
```
Directus админ-панель будет доступна на `http://localhost:8055`

Учетные данные для входа в Directus указаны в `docker-compose.yml`.

## 📁 Структура проекта

```
src/
├── main.tsx                 # Точка входа приложения
├── App.tsx                  # Корневой компонент
├── index.css                # Глобальные стили (Tailwind)
├── components/              # Переиспользуемые компоненты
│   ├── modals/             # Модальные окна
│   ├── widgets/            # Виджеты интерфейса
│   ├── sheets/             # Боковые панели
│   └── main/               # Основные компоненты
├── pages/                   # Страницы приложения
│   ├── CalendarPage.tsx    # Календарь тренировок
│   ├── ExercisesPage.tsx   # Выбор упражнений
│   ├── MyExercisesPage.tsx # Отслеживание тренировки
│   ├── ProfilePage.tsx     # Профиль пользователя
│   └── SettingsPage.tsx    # Настройки
├── services/                # API клиенты
│   ├── authApi.ts          # Аутентификация
│   ├── workoutsApi.ts      # Работа с тренировками
│   └── directusApi.ts      # Интеграция с Directus
├── lib/                     # Утилиты и хелперы
│   ├── logger.ts           # Логирование
│   ├── profileStats.ts     # Расчет статистики
│   └── telegram.ts         # Работа с Telegram API
├── hooks/                   # Custom React hooks
│   └── useTelegram.ts      # Хук для Telegram
└── contexts/                # React Context для состояния

Root конфиги:
├── eslint.config.js         # Конфиг ESLint
├── tailwind.config.cjs      # Конфиг Tailwind CSS
├── postcss.config.cjs       # Конфиг PostCSS
├── tsconfig.json            # Конфиг TypeScript
└── vite.config.ts           # Конфиг Vite

Database & Docker:
├── docker-compose.yml       # Конфиг для Docker контейнеров
├── database/                # Миграции БД
├── directus-config/         # Конфиг Directus
└── uploads/                 # Папка для загрузок Directus
```

## 💻 Доступные команды

```bash
# Разработка с HMR (Hot Module Replacement)
pnpm dev

# Сборка для production
pnpm build

# Проверка кода (ESLint)
pnpm lint

# Предпросмотр production сборки
pnpm preview

# Инициализация Directus (создание таблиц)
pnpm run setup:directus
```

## 🗄 База данных

Приложение использует **Directus** как CMS для управления данными:

### Главные таблицы
- **users** - профили пользователей
- **exercises** - каталог упражнений
- **workouts** - сессии тренировок
- **workout_sets** - отдельные подходы в упражнениях

### Настройка Directus
1. Откройте `http://localhost:8055` (когда Docker запущен)
2. Войдите с учетными данными из `docker-compose.yml`
3. Перейдите в **Content Manager** для управления данными
4. Добавляйте упражнения в коллекцию **exercises**

## 🔐 Аутентификация

### Через Telegram
Приложение встроено как Telegram Web App. При открытии из чата Telegram автоматически передается информация о пользователе.

### Через Username
Для использования в браузере без Telegram появляется модаль для ввода username.

Все сессии хранятся в localStorage и синхронизируются с сервером.

## 📊 Как использовать

### Создание тренировки
1. На странице **Календарь** нажмите на нужную дату
2. На странице **Упражнения** выберите упражнения для тренировки
3. На странице **Мой тренинг** добавьте подходы:
   - Выберите упражнение
   - Введите количество повторений и вес
   - Нажмите "Добавить подход"
4. Все данные сохраняются автоматически
5. Результаты отображаются в **Профиле** в виде статистики

### Просмотр статистики
На странице **Профиль** доступна информация:
- Время использования приложения (дни)
- Общий поднятый вес
- Количество дней с тренировками

## 🔧 Развертывание на сервер

### Подготовка сервера
1. Установить Node.js и Docker
2. Клонировать репозиторий
3. Создать `.env.local` с адресом production Directus
4. Создать `docker-compose.yml` для production Directus

### Сборка для production
```bash
pnpm build
```

Это создаст оптимизированный bundle в папке `dist/`.

### Запуск на сервере
- Frontend можно хостить на любом статическом хостере (Vercel, Netlify, собственный веб-сервер)
- Backend (Directus) запускается в Docker контейнере
- Убедитесь, что `.env.local` указывает на production URL Directus

## 📚 Дополнительная информация

- **CLAUDE.md** - детальная документация для разработчиков (архитектура, tech stack)
- **ESLint** - конфигурация проверки кода с поддержкой TypeScript и React
- **TypeScript** - строгая типизация с `strict: true`
- **Tailwind CSS** - утилитарный фреймворк для стилизации

## 🐛 Отладка

Приложение использует **logger** утилиту для логирования:
```typescript
import { logger } from './lib/logger'

logger.info('Сообщение')
logger.error('Ошибка')
```

Логи сохраняются в localStorage и доступны в DevTools консоли браузера.

## 📝 Лицензия

Приватный проект.

---

**Вопросы и поддержка**: для развертывания на сервере или помощи в разработке используйте Claude Code с файлом CLAUDE.md для получения детальных инструкций.
