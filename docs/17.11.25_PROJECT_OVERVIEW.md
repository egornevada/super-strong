# Super Strong - Как это работает?

Это подробное описание приложения для отслеживания тренировок **Super Strong**, написанное простым языком для любого человека.

---

## 🎯 Что такое Super Strong?

**Super Strong** - это приложение для записи и отслеживания тренировок. Вы можете:

✅ Вести календарь своих тренировок
✅ Выбирать упражнения из каталога
✅ Записывать количество подходов и вес
✅ Видеть статистику (сколько веса подняли, сколько дней тренировались)
✅ Работать без интернета (синхронизируется при наличии сети)

---

## 🏗️ Архитектура (из чего состоит приложение)

Приложение состоит из **трех частей**, которые работают вместе:

```
┌─────────────────────────────────────────────────┐
│ ФРОНТЭНД (то что видит пользователь)           │
│ React + TypeScript + Vite                       │
│ http://localhost:5173                          │
│                                                 │
│ • Календарь с тренировками                     │
│ • Выбор упражнений                             │
│ • Ввод подходов (reps & weight)                │
│ • Профиль с статистикой                        │
└─────────────────┬───────────────────────────────┘
                  │ Запрашивает данные через API
                  ↓
┌─────────────────────────────────────────────────┐
│ БЭКЭНД (обработка данных и логика)             │
│ Python FastAPI                                  │
│ http://localhost:8000                          │
│                                                 │
│ • Аутентификация пользователей                 │
│ • Сохранение тренировок                        │
│ • Загрузка упражнений из каталога              │
│ • Расчет статистики                            │
└─────────────────┬───────────────────────────────┘
                  │ Читает/пишет данные
                  ↓
┌─────────────────────────────────────────────────┐
│ БАЗА ДАННЫХ (хранилище данных)                 │
│ PostgreSQL через Supabase                       │
│ http://localhost:54323 (Web UI)                 │
│                                                 │
│ Таблицы:                                        │
│ • users - профили пользователей                │
│ • exercises - каталог упражнений                │
│ • user_day_workouts - workout сессии           │
│ • user_day_workout_exercises - упражнения      │
│ • user_day_workout_exercise_sets - подходы     │
└─────────────────────────────────────────────────┘
```

---

## 📱 Как работает приложение (пошагово)

### Шаг 1️⃣: Вход в приложение

```
Пользователь открывает Super Strong
         ↓
Приложение загружается (main.tsx)
         ↓
Проверка: есть ли пользователь в системе?
         ├─ ДА: Используем сохраненный JWT токен
         └─ НЕТ: Показываем форму входа
         ↓
Вход через:
 ├─ Telegram (если это Telegram WebApp)
 └─ Username (если открыто в браузере)
         ↓
Backend создает/возвращает пользователя
         ↓
Приложение готово к использованию
```

### Шаг 2️⃣: Просмотр календаря

```
Пользователь видит:
 ┌────────────────────────┐
 │   Ноябрь 2024         │
 ├────────────────────────┤
 │ 15 16 17 18 19 20 21  │
 │ 🏋️ 🏋️           🏋️    │  ← Дни отмечены значками тренировок
 │ 22 23 24 25 26 27 28  │
 │     🏋️  🏋️  🏋️       │
 └────────────────────────┘

• Зеленые дни = были тренировки
• Пустые дни = нет тренировок
```

### Шаг 3️⃣: Добавление новой тренировки

```
1. Пользователь нажимает на день в календаре
         ↓
2. Приложение показывает:
   "Выбери упражнения для этого дня"
         ↓
3. Пользователь видит список упражнений:
   • Жим лежа
   • Приседания
   • Становая тяга
   • и т.д.
         ↓
4. Пользователь выбирает упражнения
         ↓
5. Приложение показит экран для каждого упражнения:

   ┌──────────────────────────────┐
   │ Жим лежа                     │
   ├──────────────────────────────┤
   │ Подход 1:                    │
   │ Вес: [100] кг                │
   │ Повторений: [10]             │
   │                              │
   │ Подход 2:                    │
   │ Вес: [105] кг                │
   │ Повторений: [8]              │
   │                              │
   │ Подход 3:                    │
   │ Вес: [110] кг                │
   │ Повторений: [6]              │
   │                              │
   │ (Нажимаем "Назад" для        │
   │  автосохранения)             │
   └──────────────────────────────┘

6. Пользователь нажимает "Назад" (на кнопку с датой)
         ↓
7. Приложение **автоматически** отправляет данные на Backend:
   POST /api/v1/supabase-workouts/session/save
   {
     "exercises": [
       {
         "exercise_id": "жим_лежа",
         "sets": [
           {"reps": 10, "weight": 100},
           {"reps": 8, "weight": 105},
           {"reps": 6, "weight": 110}
         ]
       }
     ]
   }
         ↓
8. Backend сохраняет в БД PostgreSQL
         ↓
9. Приложение показывает сообщение "Сохранено ✓"
```

### Шаг 4️⃣: Просмотр профиля

```
Пользователь нажимает на кнопку "Профиль"
         ↓
Приложение считает статистику:

┌─────────────────────────────────────┐
│ Профиль пользователя                │
├─────────────────────────────────────┤
│ 📊 Статистика:                      │
│                                     │
│ Всего поднято:   12,500 кг          │
│ Дней в приложении: 45 дней         │
│ Последняя тренировка: 2 часа назад │
│                                     │
│ [⚙️ Настройки] [🚪 Выход]          │
└─────────────────────────────────────┘
```

---

## 🔧 Технические детали (для разработчиков)

### Frontend (React + TypeScript)

**Что находится в `frontend/src/`:**

```
frontend/src/
├── pages/                    # Экраны приложения
│   ├── CalendarPage.tsx     # Календарь тренировок
│   ├── ExercisesPage.tsx    # Выбор упражнений
│   ├── MyExercisesPage.tsx  # Ввод подходов и весов
│   ├── DayDetailPage.tsx    # Просмотр тренировок за день
│   ├── ProfilePage.tsx      # Профиль с статистикой
│   ├── SettingsPage.tsx     # Выход и удаление аккаунта
│   └── BugReportPage.tsx    # Отправка отчетов об ошибках
│
├── components/              # Переиспользуемые компоненты
│   ├── PageLayout.tsx       # Основной layout с header и footer
│   ├── Snackbar.tsx         # Уведомления (сохранено, ошибка и т.д.)
│   ├── LoadingScreen.tsx    # Экран загрузки
│   ├── SheetRenderer.tsx    # Модальные окна и sheet'ы
│   └── ...остальные компоненты
│
├── services/                # API клиенты
│   ├── authApi.ts           # Вход, выход, аутентификация
│   ├── supabaseApi.ts       # Прямая работа с БД Supabase
│   ├── workoutsApi.ts       # Сохранение, обновление тренировок
│   ├── directusApi.ts       # Загрузка каталога упражнений
│   └── ...
│
├── contexts/                # React Context для состояния
│   ├── UserContext.tsx      # Текущий пользователь
│   ├── SheetContext.tsx     # Модали и sheet'ы
│   └── ...
│
├── lib/                     # Утилиты и вспомогательные функции
│   ├── api.ts               # HTTP клиент для запросов
│   ├── telegram.ts          # Интеграция с Telegram WebApp
│   ├── profileStats.ts      # Расчет статистики
│   └── ...
│
└── App.tsx                  # Главный компонент приложения
```

**Основной поток данных (App.tsx):**

```typescript
// 1. При загрузке приложения:
useEffect(() => {
  // Проверить аутентификацию
  const user = await authApi.getOrCreateUser();

  // Загрузить упражнения из Directus
  const exercises = await directusApi.fetchExercises();

  // Загрузить тренировки с сервера
  const workouts = await workoutsApi.fetchWorkouts();

  // Сохранить в состояние
  setUser(user);
  setExercises(exercises);
  setWorkouts(workouts);
});
```

### Backend (Python FastAPI)

**Что находится в `backend/app/`:**

```
backend/app/
├── main.py                  # FastAPI приложение и маршруты
├── config.py                # Конфигурация (переменные окружения)
├── database.py              # Подключение к PostgreSQL
│
├── models/                  # ORM модели (структуры БД)
│   ├── user.py              # Модель User
│   ├── workout.py           # Модель Workout
│   └── exercise.py          # Модель Exercise
│
├── routes/                  # API endpoints (маршруты)
│   ├── auth.py              # POST /auth/telegram, /auth/verify
│   ├── supabase_users.py    # POST /supabase-users/get-or-create
│   ├── supabase_workouts.py # POST /supabase-workouts/session/save
│   ├── directus.py          # GET /directus (упражнения)
│   ├── statistics.py        # GET /statistics (статистика)
│   └── ...
│
├── services/                # Бизнес-логика
│   ├── auth.py              # Логика аутентификации
│   ├── supabase_users.py    # Логика работы с пользователями
│   ├── supabase_workouts.py # Логика сохранения тренировок
│   └── ...
│
└── schemas/                 # Pydantic схемы (валидация)
    ├── auth.py              # Схемы для аутентификации
    ├── supabase_user.py     # Схемы для пользователя
    ├── supabase_workout.py  # Схемы для тренировок
    └── ...
```

---

## 🚀 Как запустить локально

### Требования

- **Node.js 18+** (или nvm - это менеджер версий Node)
- **pnpm** (менеджер пакетов для Node)
- **Docker + Docker Compose** (для БД и Redis)
- **Python 3.9+** (для бэкэнда, но используется Docker)

### Запуск (3 терминала одновременно)

**Терминал 1: Фронтэнд**

```bash
cd super-strong/frontend
pnpm install          # Установить зависимости (делается один раз)
pnpm dev              # Запустить dev сервер
# Откроется на http://localhost:5173
```

**Терминал 2: Docker контейнеры (БД + Backend)**

```bash
cd super-strong
docker-compose -f docker-compose.dev.yml up
# Backend будет на http://localhost:8000
# Supabase Studio на http://localhost:54323
```

**Терминал 3: Supabase (если нужно)**

```bash
cd super-strong
supabase start
# Локальная БД Supabase
```

### Переменные окружения

**Frontend** (уже настроена в `.env.local`):
```
VITE_API_URL=http://localhost:8000/api/v1
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=...
```

**Backend** (в `backend/.env`):
```
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
SUPABASE_URL=http://localhost:54321
ENVIRONMENT=development
```

---

## 📋 API endpoints (как фронтэнд общается с бэкэндом)

### Аутентификация

```
POST /api/v1/auth/telegram
Вход через Telegram WebApp

Входные данные:
{
  "init_data": "telegram_data..."
}

Ответ:
{
  "access_token": "jwt_token...",
  "user": {...}
}
```

```
POST /api/v1/supabase-users/get-or-create
Создание пользователя по username

Входные данные:
{
  "username": "john_doe"
}

Ответ:
{
  "id": "user_uuid",
  "username": "john_doe",
  "created_at": "..."
}
```

### Тренировки

```
POST /api/v1/supabase-workouts/session/save
Сохранить новую тренировку

Входные данные:
{
  "user_day_id": "uuid",
  "exercises": [
    {
      "exercise_id": "uuid",
      "sets": [
        {"reps": 10, "weight": 100},
        {"reps": 8, "weight": 110}
      ]
    }
  ]
}

Ответ:
{
  "success": true,
  "session_id": "uuid"
}
```

```
DELETE /api/v1/supabase-workouts/session/{session_id}
Удалить тренировку

Ответ:
{
  "success": true
}
```

### Упражнения

```
GET /api/v1/directus
Загрузить все упражнения из каталога

Ответ:
[
  {
    "id": "exercise_1",
    "name": "Жим лежа",
    "category": "Грудь",
    "description": "..."
  },
  ...
]
```

```
GET /api/v1/statistics/daily
Получить дневную статистику

Ответ:
{
  "total_weight": 12500,
  "total_reps": 450,
  "total_sets": 45
}
```

---

## 🔐 Как работает безопасность

### JWT токены

Когда пользователь входит, приложение получает **JWT токен**. Это просто строка, которая доказывает что пользователь аутентифицирован.

```
Пользователь входит
         ↓
Backend создает JWT: "eyJhbGciOiJIUzI1NiIs..."
         ↓
Frontend сохраняет в localStorage
         ↓
Во всех запросах отправляет в заголовке:
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
         ↓
Backend проверяет JWT и выполняет запрос
         ↓
Если JWT неверный → ошибка 401 Unauthorized
```

### Row Level Security (RLS) в Supabase

Supabase имеет встроенную защиту: пользователь может видеть только свои данные.

```sql
-- Пример: пользователь может видеть только свои тренировки
CREATE POLICY "users can view own workouts"
ON user_day_workouts
FOR SELECT
USING (auth.uid() = user_id);
```

---

## 🔄 Как работает offline режим

Приложение может работать без интернета! Вот как:

```
1. При загрузке приложение:
   • Загружает данные с сервера
   • Сохраняет в localStorage браузера

2. Если интернет отключится:
   • Пользователь может добавлять тренировки локально
   • Все сохраняется в localStorage

3. Когда интернет вернется:
   • Приложение автоматически синхронизирует все pending запросы
   • Отправляет на сервер
   • Обновляет локальные данные
```

---

## 📊 Основные функции

### ✅ Функции которые работают

1. **Календарь тренировок** - видно все дни когда были тренировки
2. **Добавление упражнений** - выбор из каталога
3. **Ввод подходов и весов** - отслеживание прогресса
4. **Просмотр профиля** - статистика пользователя
5. **Аутентификация** - вход через Telegram или username
6. **Синхронизация данных** - offline/online режимы работают
7. **Удаление тренировок** - если ошибка можно исправить

### 🚧 Функции которые можно добавить в будущем

- Экспорт тренировок в PDF/Excel
- Графики прогресса
- Рекомендации и планы тренировок
- Социальные функции (друзья, сравнение прогресса)
- Push уведомления напоминания о тренировках
- Синхронизация с Apple Health / Google Fit
- Голосовой ввод весов

---

## 🐛 Debugging и логирование

Если что-то не работает:

**1. Проверить логи в браузере:**
```
F12 → Console (вкладка консоли)
Там видны ошибки и логи приложения
```

**2. Проверить Network запросы:**
```
F12 → Network
Там видно какие запросы отправляет на сервер
```

**3. Проверить localStorage:**
```
F12 → Application → Local Storage
Там видны сохраненные данные
```

**4. Проверить логи бэкэнда:**
```
docker-compose logs -f backend
Там видны все запросы и ошибки на сервере
```

**5. Проверить БД:**
```
http://localhost:54323 (Supabase Studio)
Там можно просмотреть все таблицы и данные
```

---

## 📚 Дополнительные ресурсы

- **React документация**: https://react.dev
- **FastAPI документация**: https://fastapi.tiangolo.com
- **Supabase документация**: https://supabase.com/docs
- **PostgreSQL документация**: https://www.postgresql.org/docs
- **Docker документация**: https://docs.docker.com

---

## ✨ Итого

Super Strong - это полнофункциональное веб-приложение которое состоит из:

| Часть | Технология | Для чего |
|-------|-----------|---------|
| **Фронтэнд** | React + TypeScript + Vite | Интерфейс для пользователя |
| **Бэкэнд** | Python FastAPI | Обработка данных и логика |
| **БД** | PostgreSQL (Supabase) | Хранилище данных |
| **Кеш** | Redis | Быстрое получение популярных данных |
| **Каталог упражнений** | Directus | Источник упражнений |

**Главная фишка**: Работает offline, синхронизируется при интернете, безопасно, быстро.

**Для простого пользователя**: Просто приложение для записи тренировок с красивым интерфейсом.

**Для разработчика**: Современный стек технологий с асинхронностью, ORM, REST API, БД.

---

*Создано: 17.11.25*
*Версия: 1.0.3*
*Автор: Claude Code*
